import 'package:flutter/material.dart';

class EventoPage extends StatelessWidget {
  const EventoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Eventos Page'),
    );
  }
}